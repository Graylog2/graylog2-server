type = "fixed"
message = "Fix breaking change in Geo-Location Processor fields."

issues = ["12909", "13202", "13203"]
pulls = ["13094"]

details.user = """
Changes in the IP Geo-Location Processor caused the fields generated to be different from what was previously expected.
The new behavior and fields generated by the processor can be found in the processor's [documentation](https://docs.graylog.org/docs/geolocation).
"""
