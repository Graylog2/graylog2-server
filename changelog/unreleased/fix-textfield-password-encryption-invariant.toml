type = "fixed"
message = "Warn at startup when an input field is declared with IS_PASSWORD but without encryption at rest, and mask additional encrypted-at-rest secret fields in the UI."

issues = []
