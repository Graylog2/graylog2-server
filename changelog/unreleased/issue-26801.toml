type = "added"
message = "Populate the GIM `event_sequence` field with a receiver-specific sequence number for Collector messages, so messages sharing a millisecond timestamp can be sorted."

issues = ["26801"]
