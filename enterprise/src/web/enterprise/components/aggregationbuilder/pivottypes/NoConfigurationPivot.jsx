import React from 'react';
import PropTypes from 'prop-types';

const NoConfigurationPivot = () => (
  <div>
    This pivot has no configuration options.
  </div>
);

NoConfigurationPivot.propTypes = {};

export default NoConfigurationPivot;
