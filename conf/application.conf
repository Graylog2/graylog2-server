# This is the main configuration file for the application.
# ~~~~~

# graylog2-server REST URI
graylog2-server.uri="http://127.0.0.1:12900/"

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
# If you deploy your application to several instances be sure to use the same key!
application.secret="9wIJSg4]OF/POYCDKukHj][pnbFA^2=qv[a3bmC=36jw4/00O32u[WxV>DlHl4lr"

# the local admin user's name and MD5 of its password "123123123"
# this is used to be able to log in to edit configuration even if you cannot connect to the graylog2-servers from above.
# typically this might happen when you rename the hostnames
local-user.name="localadmin"
local-user.password-md5="f5bb0c8de146c67b44babbf4e6584cc0"

# The application languages
# ~~~~~
#application.langs="en"

# Logger
# ~~~~~
# You can also configure logback (http://logback.qos.ch/), by providing a logger.xml file in the conf directory .

# Root logger:
logger.root=INFO

# Logger used by the framework:
logger.play=DEBUG

# Logger provided to your application:
logger.application=DEBUG

