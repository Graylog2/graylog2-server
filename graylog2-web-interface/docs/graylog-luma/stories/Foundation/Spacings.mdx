import { Meta, Unstyled } from '@storybook/addon-docs/blocks';
import { Alert } from 'components/bootstrap';
import { SpacingsDoc } from './Spacings';

<Meta title="Foundation/Spacings" />

# Spacings

<Unstyled>
  <Alert bsStyle="warning">
    {'Spacing theme tokens are for '}
    <strong>component authors</strong>
    {' building new reusable components in '}
    <code>components/common</code>
    {
      '. When using an existing component, its internal spacing is already built in. Do not apply spacing tokens manually outside of reusable components.'
    }
  </Alert>
</Unstyled>

<Unstyled>
  <SpacingsDoc />
</Unstyled>
