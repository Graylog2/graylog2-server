import * as React from 'react';

const HelloFromTypeScriptPage = () => {
  const foo: number = 42;

  return (
    <span>
      Hello from TypeScript: {foo}
    </span>
  );
};

export default HelloFromTypeScriptPage;
