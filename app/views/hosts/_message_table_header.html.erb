<div id="hosts-delete">
  <% if Configuration.allow_deleting %>
    <%= link_to "Delete this host", host_path(@host), :method => :delete, :confirm => "Really delete this host with all it's messages?", :method => :delete %>
  <% end %>
</div>

<h1>Host <span class="highlighted"><%=h @host.host %></span></h1>

Messages from <span class="highlighted"><%= gl_date(Time.at(@last_message.created_at).to_s) %></span> available
for this host.
