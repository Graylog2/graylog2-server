<#if _title>
    Event limit reached
</#if>

<#if _description>
    Event limit »${event_limit}« reached for event definition »${event_definition_title}«. Try to use a more specific search query or use aggregations. Otherwise try to raise the limit.
</#if>
