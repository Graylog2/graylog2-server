<#if _title>
    Event limit reached
</#if>

<#if _description>
    Event limit »${event_limit}« reached for event definition »${event_definition_title}«. Increase event limit or aggregate events.
</#if>
