rule "mapUpsert"
when
  true
then
  let newValue = to_map(parse_json("{\"k1\":\"v1\",\"k2\":\"v2\"}"));
  map_upsert(newValue, "k1", "v11");
  map_upsert(newValue, "k3", "v3");
  set_fields(newValue);
end
